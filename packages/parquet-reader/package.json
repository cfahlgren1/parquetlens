{
  "name": "@parquetlens/parquet-reader",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap --external @duckdb/duckdb-wasm",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.32.0",
    "apache-arrow": "^17.0.0"
  }
}
