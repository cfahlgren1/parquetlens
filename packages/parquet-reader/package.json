{
  "name": "@parquetlens/parquet-reader",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "apache-arrow": "^21.1.0",
    "parquet-wasm": "^0.7.1"
  }
}
